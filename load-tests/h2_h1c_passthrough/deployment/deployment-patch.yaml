apiVersion: "apps/v1"
kind: Deployment
metadata:
  name: no-name
spec:
  template:
    metadata:
      labels:
        logs: "true"
    spec:
      containers:
      - name: "h2-h1c-passthro-deployment"
        imagePullPolicy: Always

